# Figure 15 三档 120 帧测试（2026-09-17）

## 输入与结论

沿用 [27 帧三档测试](AGIPC_FIG15_THREE_SCALES_2026-09-17.md) 的 100×100、220×220、300×300 平面布料 OBJ、固定 ABD 球、`E=1e6`、`dt=0.01`、`abd-cemas-srbk`，只把 `--frames` 改为 `120`。StiffGIPC 使用 `--newton-stop paper-current`；AGIPC-Core 使用 MAS32、GPU 局部检查、精确图复用和 post10。六次尝试均使用 SHA-256 为 `4F6E303507844CC66EC886A2F7A23532B04D1A004EAD9927BCAEB31043BCB6BB` 的同一个 Release 可执行文件。原始输出在 `outputs/agipc_figure15_three_scales/run120/`。

**只有约 10K 档的两个方法完成 120 帧。** 约 50K 档两方法和约 90K 的 StiffGIPC 在接触段进入持续相交线搜索；约 90K 的 AGIPC 在显存余量耗尽后停滞。下表中未完成项没有模拟总耗时或速度比，停止时的进程墙钟不是有效 benchmark。

| 布料/总顶点 | 方法 | 已完整结束帧 | 状态 | 进程墙钟 |
|---:|---|---:|---|---:|
| 10,000 / 11,647 | StiffGIPC | 120 | 正常退出 0 | 231.49 s |
| 10,000 / 11,647 | AGIPC-Core | 120 | 正常退出 0 | 289.81 s |
| 48,400 / 50,047 | StiffGIPC | 33 | 第 34 帧相交线搜索超过 304 次减半后人为停止 | 437.38 s |
| 48,400 / 50,047 | AGIPC-Core | 34 | 第 35 帧相交线搜索超过 152 次减半后人为停止 | 410.92 s |
| 90,000 / 91,647 | StiffGIPC | 30 | 第 31 帧相交线搜索至少 76 次减半后人为停止 | 439.25 s |
| 90,000 / 91,647 | AGIPC-Core | 27 | 第 28 帧显存几乎耗尽且超过 3 分钟无帧进展后人为停止 | 438.19 s |

三个相交循环都在 `GIPC::lineSearch()` 的 `while(checkInterset && isIntersected(TetMesh))` 中反复把 `alpha` 减半；该循环目前没有下界或最大次数。76 次减半已使 `alpha` 相对初值约为 `2^-76`，不能把之后的忙循环当成正常慢帧。为避免无限占用设备，确认目标进程路径后停止了这三次进程；日志及最后完整帧均保留。约 90K 的 AGIPC 在第 28 帧尝试建立约 394.16 MiB MAS 矩阵工作区，调用前报告仅约 278.26 MiB 可用、调用后报告 0 MiB；GPU 占用约 7.8/8 GiB。没有明确 CUDA OOM 错误或退出码，故标记为**容量附近停滞**，不武断归因为已证实的分配失败。

## 约 10K 档的完整结果

JSON `simulation_time_ms` 为 StiffGIPC **229.004 s**、AGIPC-Core **264.232 s**；本次单对观测 Stiff/AGIPC 比为 **0.867×**，即 AGIPC 在此长序列上约慢 15.4%。这与同网格 27 帧几乎持平的结果不同。Stiff/AGIPC 的 Newton 更新为 5,154/4,006，报告线性迭代为 410,481/513,170；最多自接触对 2,236/2,631。两者完成 120 帧、顶点有限、地面穿透均为零。

AGIPC 候选采用 4,055 次、回退 71 次，其中 70 次为 `coarse_mas_local_diagnostics_failed`，1 次为 `mapping_incomplete`；精确图复用 103 次。MAS setup/校验累计分别约 13.826/29.244 s。两方法的 10,000 个 FEM 顶点终态位置 RMS 差为 **0.048554**，最大差 **0.201178**，RMS 差占 StiffGIPC 布料 RMS 位移约 **6.91%**。此前 27 帧同档终态 RMS 差仅约 `0.000462`，说明长接触轨迹显著分化；有限值与地面零穿透不能替代完整自交和逐帧物理一致性验证。因此 0.867× 只是这次不同轨迹的运行时间比，不能视为受控的算法性能比较。

## 后续门槛

先复核约 50K、约 90K 接触开始时的几何相交与 `lineSearch()` 为什么即使步长趋零仍返回相交；为循环增加可观测的有限失败出口，而不是无条件接受相交状态。约 90K 还需要足够显存余量或明确的工作区容量处理。10K 长序列要用逐帧状态定位首次明显分叉，并检查 70 次局部诊断拒绝的残差分布。完成这些数值/资源门后，再进行 120 帧的重复计时；本轮不把未完成运行填入速度表，也不调整 `dt` 或碰撞参数来掩盖失败。
