# AGIPC 当前版简短速度对照（2026-09-16）

## 问题与范围

本轮回答两个不同的问题：当前 AGIPC-Core 与 StiffGIPC 在同一 27 帧代理场景的观测耗时，以及多项已保留优化合并后，相对优化前 AGIPC-Core 是否更快。使用 Release 二进制、16,641 FEM 节点布料、固定 ABD 球、`E=1e6`、`dt=.01`、`abd-cemas-srbk`、无 Nsight；AGIPC 为 MAS32、GPU 局部校验、精确图复用、post10。StiffGIPC 显式使用 `--newton-stop paper-current`，以匹配停止规则。所有运行均完成 27 帧、顶点有限、地面穿透 0。原始 JSON、日志及最终顶点在 `perf_diag/quick_speed_scene_*` 与 `perf_diag/quick_speed_vs_stiff_*`，不纳入 Git。

当前二进制 `gipc.exe` SHA-256 为 `4F6E303507844CC66EC886A2F7A23532B04D1A004EAD9927BCAEB31043BCB6BB`。优化前 AGIPC 二进制 `gipc_before_mas_fastpath.exe` SHA-256 为 `8F7E5458D1920D49D35676F3E252FD3ADEFF6E117077127F31C0B99511E97839`；后者已经包含粗 PCG 双内积合并，尚未包含后续 MAS 局部校验快路、setup 同步删减、图复用快路和细层双内积合并。

## 当前 AGIPC 对 StiffGIPC

同一当前二进制按 `StiffGIPC, AGIPC, StiffGIPC` 顺序运行；下面的耗时使用 JSON 中 `simulation_time_ms`，不含启动/加载阶段。

| 运行 | 模拟耗时 | Newton 更新 | 报告线性迭代 |
|---|---:|---:|---:|
| StiffGIPC 1 | 12.178 s | 177 | 7,182 |
| AGIPC-Core | 5.911 s | 72 | 3,372 |
| StiffGIPC 2 | 10.706 s | 177 | 7,179 |

观测速度比为 **1.81–2.06×**，两次 StiffGIPC 中位耗时除以单次 AGIPC 耗时约 **1.94×**。16,641 个顶点的 AGIPC/StiffGIPC 终态位置 RMS 差约 `0.0004756`，相对于 StiffGIPC RMS 位移 `0.37009` 约 `0.129%`；两次 StiffGIPC 之间 RMS 差约 `6.24e-9`。这是一组简短代理场景结果，不是论文网格、RTX 4090 或全场景的受控速度结论。两算法更新数 72/177，不应把 1.94× 解释为同一线性系统的求解器内核加速比。

## 已保留优化的合并效果

优化前与当前 AGIPC 按 `旧, 新, 新, 旧` 顺序运行相同 27 帧参数；旧二进制也开启 GPU MAS 局部校验与精确复用选项。所有候选均采用，回退及局部检查失败均为 0。接触轨迹的粗层更新数有变化，因此下面的“每次更新”只是探索性归一化，不能代替固定输入配对。

| 运行 | 模拟耗时 | Newton 更新 | 粗层更新 | 粗层累计 | MAS setup 累计 | 校验累计 |
|---|---:|---:|---:|---:|---:|---:|
| 旧 1 | 5.595 s | 70 | 97 | 1.497 s | 0.428 s | 0.365 s |
| 新 1 | 5.810 s | 77 | 104 | 1.330 s | 0.154 s | 0.528 s |
| 新 2 | 5.400 s | 67 | 94 | 1.344 s | 0.163 s | 0.524 s |
| 旧 2 | 5.663 s | 69 | 96 | 1.562 s | 0.458 s | 0.388 s |

两次中位模拟耗时旧约 5.629 s、新约 5.605 s，比例 **1.004×**，即本轮未观察到可信的场景总耗时提速。粗层累计耗时除以更新数后，中位从约 15.85 降至 13.54 ms/次（探索性约 1.17×）；setup 从 4.59 降至 1.61 ms/次，但校验从约 3.90 增至 5.33 ms/次。校验增幅可能包含 GPU 负载波动或等待转移，本轮不能确定原因。局部快路在固定矩阵的历史交错 Nsight 对照中确有 kernel 中位耗时下降约 30.5%，但该收益没有线性传递到场景总耗时。

当前分支保留的速度相关代码提交为：`e56dc1f` 粗 PCG 双内积合并、`c7600ad` MAS 局部校验快路、`8e43e57` setup 同步删减、`5bbc271` 精确图复用快路、`7789a69` 细层双内积合并。它们通过各自数值门并已组合在当前二进制中；**并非每项都被证实单独或合并后提高端到端速度**。上一轮对角占优证书、Cholesky barrier 改写、shared memory 逆缓存、求逆消元去 barrier 试验均撤回，当前不包含这些改动。

多个改动理论上可能相互抵消，也可能改善总耗时，即使某一个阶段变慢；但不能据单项 kernel 计时推断。当前组合的场景试验已回答“是否合起来更快”：在这组 27 帧样本中，**没有可分辨的端到端收益**。若要判断被撤回的候选是否在组合后变快，需要专门构建组合候选，并在相同冻结系统及同轨迹场景上对照；现有数据没有支持恢复它们的理由。
