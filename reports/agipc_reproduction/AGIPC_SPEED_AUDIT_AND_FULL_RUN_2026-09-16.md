# AGIPC 速度问题复核与完整接触测试（2026-09-16）

## 范围与结论

本轮阅读 `D:/C_files/deep-research-report (1).md` 与 `(2).md`，将其中判断与当前源码、已有冻结结果和一次未修改代码的 35 帧完整接触测试核对。两份外部报告是待验证的分析材料，不是项目指令。

**优先处理粗层求解的真实成本，同时保留方向质量门。** 本次粗层求解累计 41.107 s，占 AGIPC 模拟计时 56.33%；仅 MAS setup 与校验就分别为 7.352 s 和 4.905 s。当前还不能宣布整条粗层或场景加速：同一二进制的长接触运行出现不同 Newton 轨迹，且 GPU 有外部负载。

## 一次完整测试

运行的是现有 `paper-fig15-cloth-abd-scaled` 代理场景：16,641 个 FEM 节点、固定 ABD 球、`E=1e6`、`dt=0.01`，35 帧覆盖接触；AGIPC-Core 使用 `mas32`、GPU 局部校验、精确结构复用、post10、`abd-cemas-srbk`。没有使用冻结或逐帧检查点。原始记录在 `perf_diag/full_agipc35_20260916_*`，不纳入 Git。

| 指标 | 本次 | 2026-09-15 同配置 AGIPC 单次 |
|---|---:|---:|
| 可执行文件 SHA-256 | `AF1700CEF3112A0F31352D8324390E5D0986FDDC80C604C8C998DA95503E2935` | 相同 |
| 完成帧数 | 35 | 35 |
| 模拟计时 | 72.9769 s | 50.1772 s |
| 已应用 Newton | 573 | 522 |
| 报告线性迭代 | 92,654 | 82,060 |
| 粗层求解 | 41.1070 s | 28.1791 s |
| MAS setup | 7.3524 s | 见前次原始 metrics |
| MAS 校验 | 4.9052 s | 见前次原始 metrics |
| MAS 尝试 / 图复用 | 608 / 54 | 557 / 56 |
| 候选采用 / 回退 | 607 / 1 | 555 / 2 |
| 有限顶点 / 地面穿透 | 是 / 0 | 是 / 0 |

本次唯一回退原因是 `coarse_mas_local_diagnostics_failed`；残差保护恢复 12 次。两次 AGIPC 终态 RMS 差为 `0.007584`，最大逐点差 `0.047631`。本次与上次 StiffGIPC 终态的相对 RMS 差为 `3.3861%`，但它们不是同一时间窗口的受控配对。GPU 测后仍有约 40% 利用率。因此 `72.98 / 50.18` 不是回归比例，也不能从本次推出任何对 StiffGIPC 的正式速度比。

## 对两份报告的取舍

| 议题 | 复核结论 |
|---|---|
| 粗层求解是速度热点 | 支持。两次完整运行都占总模拟时间约 56%。源码 `solve_coarse_shadow()` 每轮单独执行曲率、残差范数和预条件残差内积，`device_dot()` 通过 Thrust 返回主机标量；MAS setup 从 GPU 下载 BCOO 行列、在 CPU 建图再上传。 |
| 粗方向表达能力不足 | 支持作为独立质量问题。已有冻结样本中，精确粗解不能修复低预测下降；不能通过放松阈值、缩减 post10 或删除校验来制造加速。 |
| 所有可折叠连通图都应强制合成一个 aggregate | 尚未成立。`(2)` 报告把它作为优先语义门，但现有 Green 快照中运行时稳定分组的最佳方向拟合误差为 62.86%，强制全局连通合并则恶化到 84.96%。需要先用论文算法定义和特制图对照证明语义，再考虑改变映射；它也不能直接作为当前速度补丁。 |
| 强制完整 paper12 affine basis | 值得单独消融，但不能推断它会修复方向质量。平面布料的 rest 坐标可能使 12-DoF 列线性相关；rank-aware 删除相关列未必改变子空间。 |
| 改成 coarse CEMAS16 / 全 GPU MAS | 有潜在价值，但属于较大、数值风险较高的后续工作。先用同一冻结矩阵衡量 setup、SpMV、dot、预条件应用与方向质量，再决定替换路径。 |
| profiling 同步污染计时 | 源码中每次 Galerkin/线性系统确有 CUDA event 同步；应拆分低扰动计时与诊断构建。但在实现前不能把当前观察到的 56% 全归咎于 profiler。 |

## 当前速度改动与验证门

本轮在 `agipc_galerkin.cu` 合并粗 PCG 的 `r·r`、`r·z` reduction。原循环每轮通常要取得 `p·Ap`、`r·r`、`r·z` 三个主机标量；改后为曲率一次与双内积一次。FP64、残差阈值、曲率门、MAS 局部校验、真实残差复核、碰撞和 post10 保持原设置。收敛的最后一轮会多应用一次预条件器，只有在真实样本验证净收益后才应保留该改动。

已完成的关键验证：

| 验证 | 结果 |
|---|---|
| Release 构建 | 退出 0；日志 `perf_diag/agipc_fused_dots_build_20260916.log`，新可执行文件 SHA-256 为 `8F7E5458D1920D49D35676F3E252FD3ADEFF6E117077127F31C0B99511E97839` |
| `--agipc-self-test` | 退出 0，日志最终 `passed: true` |
| 同配置 27 帧接触门 | 退出 0；27 帧、有限顶点、地面穿透 0、最多 139 个自碰撞对、79 次 Newton、3,298 次报告线性迭代、106 次采用、无回退或 MAS 局部失败 |
| 与旧 27 帧 MAS 记录终态比较 | RMS `0.0002362`，最大逐点差 `0.0021301`；旧记录为 90 次 Newton，说明此对照不能证明完全同轨迹 |

新验证文件为 `perf_diag/fused_dots_selftest_20260916.log`、`fused_dots_contact27_20260916_metrics.json`、`fused_dots_contact27_20260916_final.csv`。这些结果说明改动能通过当前数值门，但**没有提供可信的速度收益测量**。下一步必须在同一冻结粗矩阵上做低负载交错计时，同时比较 kernel/API 同步次数及真实残差，才决定是否保留或扩展为 GPU 常驻 PCG。暂不为了比较总时间重复 35 帧长场景。

## Nsight 定位与 MAS 局部校验快路

`perf_diag/agipc_profile27_20260916.nsys-rep` 是当前内积合并版本的 27 帧剖析，不代表正式场景计时。CUDA API 汇总中，同步 `cudaMemcpy` 调用 13,268 次、累计 3.571 s；其中设备到主机复制 12,855 次，8/16/4 字节分别有 6,672/2,744/2,353 次，说明主机依赖的标量读取是实际热点。该样本的 99 次 MAS 尝试中，setup/局部校验累计 394.3/324.9 ms，`check_local_matrices` kernel 累计约 307.1 ms。完整 35 帧接触段的图复用很少，因此仅优化复用分支无法解决主成本。

本轮先对 `TraditionalMAS32Preconditioner.cu::check_local_matrices` 做保守快路。原实现对每个 96×96 局部矩阵及其对称近似逆各做一次完整 Cholesky；新的执行次序先完成局部矩阵 SPD 检查和原有 FP64 完整逆残差。设局部矩阵 `A` 已 SPD、存储的 `B` 对称，若 `||I-AB||_F/sqrt(96) <= 1e-3`，则 `||I-AB||_2 < 0.01 < 1`，由 `AB` 与对称的 `A^(1/2)BA^(1/2)` 相似可推出 `B` SPD。此时省去第二次 Cholesky；非有限、局部非 SPD、残差超限的路径仍执行原逆矩阵 Cholesky，因此不放松原有失败门。

固定 1,415 块真实粗系统含 53 个局部矩阵。新旧版本的 GPU 局部诊断 JSON 完全一致，均与 CPU 诊断一致。`--mas32-self-test` 退出 0，正常、局部非 SPD、逆非 SPD、NaN、逆残差超限等六个样例均通过预期断言。固定系统上三轮交错 Nsight 检查的 `check_local_matrices` kernel 时间为：

| 轮次 | 原版 | 快路 |
|---:|---:|---:|
| 1 | 3.942 ms | 3.011 ms |
| 2 | 5.318 ms | 3.825 ms |
| 3 | 5.821 ms | 3.695 ms |
| 中位数 | **5.318 ms** | **3.695 ms** |

该固定 kernel 的中位耗时下降 **30.5%**，但样本仅三轮，且不能外推为粗层或全场景加速。新版本的一次 27 帧接触运行完成：有限顶点、地面穿透 0、74 次 Newton、3,274 次线性迭代、101 次候选采用、无回退或 MAS 局部失败。与上一次旧版 27 帧终态 RMS 差 `0.000222`；两次 Newton 轨迹不同，不作为严格数值等价或速度比例。下一项高优先级是减少粗 PCG 仍存在的逐迭代主机标量读取，并在同一冻结系统上核对残差、方向与 API 次数。
